// import React from "react";
// import PortableText from "react-portable-text";

// type Props = {
//   portableTextContent: string;
// };

// const portableText = (props: Props) => {
//   return <PortableText content={props.portableTextContent} />;
// };

// export default portableText;
import React from "react";

type Props = {};

const portableText = (props: Props) => {
  return <div>portableText</div>;
};

export default portableText;
